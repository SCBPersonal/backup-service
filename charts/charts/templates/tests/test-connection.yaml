apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backup-orchestrator-service-helm.fullname" . }}-test-connection"
  labels:
    {{- include "backup-orchestrator-service-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backup-orchestrator-service-helm.fullname" . }}:{{ .Values.service.port }}/actuator/health']
  restartPolicy: Never